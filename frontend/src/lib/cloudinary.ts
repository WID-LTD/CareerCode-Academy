export function isCloudinaryUrl(url: string): boolean {
  return false;
}

export function optimizeImageUrl(url: string, _width = 400, _height = 250): string {
  return url || '';
}

export function optimizeVideoThumbnail(url: string, _width = 640, _height = 360): string {
  return url || '';
}

